node_modules/
.DS_Store
_preview/

# output del generatore: si ricostruisce con `npm run build`
dist/
anteprime-email/

# archivio locale di disponibilita' (fallback senza KV, solo per sviluppo)
.data/
